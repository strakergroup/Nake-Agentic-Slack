# Changelog

All notable changes to this project will be documented in this file.

## Changes

- Changed: Updated `escape_slack_emoji` and `unescape_slack_emoji` functions to use `<span translate="no">` tags instead of `<x i={n}/>` placeholder tags for wrapping emoji content to prevent translation (Wade Norman, 2026-01-06)
